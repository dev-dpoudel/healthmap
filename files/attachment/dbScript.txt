$ sudo mkdir -p /opt/dbdata/django-polls-db
$ sudo chown 26:26 /opt/dbdata/django-polls-db
$ sudo setfacl -m u:26:-wx /opt/dbdata/django-polls-db
$ sudo semanage fcontext -a -t container_file_t /opt/dbdata/django-polls-db
$ sudo restorecon -v /opt/dbdata/django-polls-db


sudo podman run  -it --name contextdb -e POSTGRESQL_USER=alfaaz -e POSTGRESQL_PASSWORD=Alfaaz1124! -e POSTGRESQL_DATABASE=symphony  5432:5432 -v /home/alfaaz/Programs/Data/context-db/:/var/lib/pgsql/data:Z registry.redhat.io/rhel8/postgresql-10

sudo podman rm contextdb





Description=Symphony Application

After=contextdb.service

[Service]
Type=simple
TimeoutStartSec=30s
ExecStartPre=-/usr/bin/podman rm "services"

ExecStart=/usr/bin/podman run --rm -it -p 8000:8000 -v /home/alfaaz/Programs/Microservices:/home/alfaaz/Programs/Microservices:Z --name services symphony/services

#ExecStart=/usr/bin/podman run --name services --net host symphony/services

ExecReload=-/usr/bin/podman stop "symphony/services"
ExecReload=-/usr/bin/podman rm "symphony/services"
ExecStop=-/usr/bin/podman stop "symphony/services"
Restart=always
RestartSec=30

[Install]
WantedBy=multi-user.target

